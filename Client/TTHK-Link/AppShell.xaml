<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="TTHK_Link.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:pages="clr-namespace:TTHK_Link.Pages"
    Title="TTHK_Link"
    FlyoutBehavior="{Binding FlyoutBehavior}">

    <!-- auth pages (not shown in flyout) -->
    <ShellContent
        Route="login"
        Title="Login"
        Shell.FlyoutItemIsVisible="False"
        ContentTemplate="{DataTemplate pages:LoginPage}" />

    <ShellContent
        Route="register"
        Title="Register"
        Shell.FlyoutItemIsVisible="False"
        ContentTemplate="{DataTemplate pages:RegisterPage}" />

    <!-- main -->
    <FlyoutItem Title="Courses" Route="courses" IsVisible="{Binding IsAuthenticated}">
        <ShellContent ContentTemplate="{DataTemplate pages:CoursePage}" />
    </FlyoutItem>

    <FlyoutItem Title="Profile" Route="profile" IsVisible="{Binding IsAuthenticated}">
        <ShellContent ContentTemplate="{DataTemplate pages:ProfilePage}" />
    </FlyoutItem>

    <Shell.FlyoutFooter>
        <Grid Padding="12" IsVisible="{Binding IsAuthenticated}">
            <Button Text="Logout" Command="{Binding LogoutCommand}" />
        </Grid>
    </Shell.FlyoutFooter>

</Shell>
