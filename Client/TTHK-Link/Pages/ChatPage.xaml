<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="TTHK_Link.Pages.ChatPage">

    <Grid Padding="10" RowDefinitions="*,Auto" RowSpacing="10">

      <CollectionView Grid.Row="0"
                ItemsSource="{Binding Items}"
                Margin="0,0,0,10">
    <CollectionView.ItemTemplate>
        <DataTemplate>
            <Grid Padding="6">
                <Frame Padding="10"
                       CornerRadius="18"
                       HasShadow="False"
                       MaximumWidthRequest="280"
                       HorizontalOptions="Start"
                       BackgroundColor="#E9E9EB">

                    <Frame.Triggers>
                        <DataTrigger TargetType="Frame"
                                     Binding="{Binding IsMine}"
                                     Value="True">
                            <Setter Property="HorizontalOptions" Value="End" />
                            <Setter Property="BackgroundColor" Value="#2A7FFF" />
                        </DataTrigger>
                    </Frame.Triggers>

                    <VerticalStackLayout Spacing="4">
                        <!-- Имя показываем только если НЕ моё -->
                        <Label Text="{Binding SenderName}"
                               FontSize="12"
                               Opacity="0.7"
                               IsVisible="True">
                            <Label.Triggers>
                                <DataTrigger TargetType="Label"
                                             Binding="{Binding IsMine}"
                                             Value="True">
                                    <Setter Property="IsVisible" Value="False" />
                                </DataTrigger>
                            </Label.Triggers>
                        </Label>

                        <Label Text="{Binding Msg}"
                               FontSize="15"
                               TextColor="Black">
                            <Label.Triggers>
                                <DataTrigger TargetType="Label"
                                             Binding="{Binding IsMine}"
                                             Value="True">
                                    <Setter Property="TextColor" Value="White" />
                                </DataTrigger>
                            </Label.Triggers>
                        </Label>

                        <Label Text="{Binding CreatedAt, StringFormat='{0:HH:mm}'}"
                               FontSize="10"
                               Opacity="0.6"
                               HorizontalOptions="End"
                               TextColor="Gray" />
                    </VerticalStackLayout>
                </Frame>
            </Grid>
        </DataTemplate>
    </CollectionView.ItemTemplate>
</CollectionView>
        
    <!-- Sisestus + nupp -->
    <Grid Grid.Row="1" ColumnDefinitions="Auto,*,Auto" ColumnSpacing="10" Padding="6">
        <Button Text="＋" WidthRequest="40" HeightRequest="40" CornerRadius="20"/>

        <Frame Grid.Column="1" Padding="10,6" CornerRadius="18" HasShadow="False" BackgroundColor="#F2F3F5">
            <Entry Placeholder="Сообщение..." BackgroundColor="Transparent"/>
        </Frame>

        <Button Grid.Column="2" Text="➤" WidthRequest="40" HeightRequest="40" CornerRadius="20"
                BackgroundColor="#2A7FFF" TextColor="White"/>
    </Grid>

</Grid>
</ContentPage>